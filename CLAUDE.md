# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

这是一个 Obsidian 医学知识库 (vault)，使用中文编写。当前重心是**内科学、外科学、妇产科学、儿科学**四门核心临床学科。默认笔记创建路径为 `01 Notes/`，附件路径为 `04 Attachments/`。

## 目录结构

| 目录 | 用途 |
|------|------|
| `00 MOC/` | Map of Content — 各学科的内容索引，列出笔记间的层级关系 |
| `01 Notes/` | 所有具体笔记（疾病、药物、概念），每个 `.md` 一个主题 |
| `02 Courses/` | 课程资料 — 按学科分子目录存放课本 PDF、课程 PPT、教学大纲 |
| `03 Templates/` | 笔记模板 — 内科、外科、儿科、妇产科、寄生虫、病理学、药物、遗传病 |
| `04 Attachments/` | 图片附件，全部为 `.webp` 格式 |
| `05 Excalidraw/` | Excalidraw 绘图文件 |

## 笔记写作工作流

以**课本为内容依据**，以**教学大纲为写作目标**，以**PPT 为插图来源**。

### 数据源的三层分工

| 数据源 | 角色 | 说明 |
|--------|------|------|
| 课本 PDF（`02 Courses/学科/`） | **内容依据** | 笔记正文的知识来源，严格按课本内容撰写 |
| 教学大纲（`02 Courses/学科/XX教学大纲.md`） | **写作目标** | 定义需要覆盖的疾病和知识点范围，大纲中列为"掌握"的内容重点展开，"了解"的内容可简略 |
| 课程 PPT（`02 Courses/学科/`） | **插图来源** | 提取 PPT/PDF 中的示意图、分类表、流程图作为笔记配图 |

### 写作流程

1. **确定范围**：读取对应学科的 `XX教学大纲.md`，确定要写哪些疾病和知识点
2. **查索引定位**：读取 `02 Courses/学科/_课本索引.md`，找到目标章节的页码
3. **提取课本**：用 PyMuPDF 按页码范围提取课本 PDF 的正文内容
   ```bash
   python -c "import fitz; doc=fitz.open('课本.pdf'); [print(doc[i].get_text()) for i in range(起始页-1, 结束页)]"
   ```
4. **套用模板**：根据学科选择 `03 Templates/` 中的对应模板（内科→内科.md，外科→外科.md 等）
5. **撰写笔记**：以课本内容填充模板框架，输出到 `01 Notes/疾病名.md`
6. **提取插图**：从课程 PPT/PDF 中提取关键图表，WebP 格式保存至 `04 Attachments/`
7. **添加 wikilink（双向链接）**：见下方 [[#Wikilink 双向链接工作流]]
8. **更新 MOC**：在 `00 MOC/` 的对应学科 MOC 文件中添加新笔记链接

### Wikilink 双向链接工作流

Obsidian 的核心价值在于笔记间的网状关联。每写完一篇新笔记，必须完成以下双向链接步骤：

**A. 正向链接（新笔记 → 已有笔记）**

在新笔记正文中，为出现的已有知识点添加 `[[wikilink]]`。操作方法：

```bash
ls "01 Notes/" | sed 's/\.md$//'
```
列出所有已有笔记名，与新笔记内容交叉比对，找出可链接的概念。例如：
- 新笔记"骨折概述"中提到"化脓性骨髓炎"→ 若已有 `化脓性骨髓炎.md`，则写 `[[化脓性骨髓炎]]`
- 新笔记中提到"休克"→ 若已有对应笔记，则写 `[[休克]]`

**B. 反向链接（已有笔记 → 新笔记）**

检查与新笔记主题相关的已有笔记，在它们的适当位置添加指向新笔记的链接。例如：
- 写了"骨折概述"后，检查 `00 MOC/外科学.md` 是否已添加（步骤 8）
- 若已有相关笔记（如某具体骨折），应在该笔记的"参见"处添加 `[[骨折概述]]`

**C. 链接密度**

- 每个主要章节至少出现 1~2 个 wikilink
- 不要在同一个段落重复链接同一概念（首次出现时链接即可）
- 不要为通用词汇（如"疼痛""发热"）创建链接，只为有对应笔记页面的医学概念添加
- alias 用法：若笔记有 `aliases` frontmatter，可用 `[[笔记名|显示文本]]` 格式

**D. 写后自检清单**

写完每篇笔记后，执行以下检查：
1. `grep -o '\[\[.*\]\]' "01 Notes/新笔记.md" | wc -l` — 确认 wikilink 数量 ≥ 3
2. 检查是否有已存在但未添加 wikilink 的知识点
3. 确认 MOC 已更新
4. 确认至少有一篇已有笔记添加了反向链接（如 MOC 或相关主题笔记）

### 课本章节索引

每个学科目录下有一个 `_课本索引.md` 文件（文件名以 `_` 开头便于排序靠前），记录了对应课本的完整目录结构和页码。四个核心学科均已建立：

| 学科 | 索引文件 | 课本文件 |
|------|----------|----------|
| 内科学 | `02 Courses/内科学/_课本索引.md` | `《内科学 第10版》.pdf` |
| 外科学 | `02 Courses/外科学/_课本索引.md` | `《外科学》（第10版）.pdf` |
| 妇产科学 | `02 Courses/妇产科学/_课本索引.md` | `《妇产科学》（第10版）.pdf` |
| 儿科学 | `02 Courses/儿科学/_课本索引.md` | `《儿科学》（第10版）.pdf` |

**使用方式**：写笔记前先 Read 索引文件，找到目标章节的起始页码（如肝疾病 p.481），然后用 PyMuPDF 一次性提取该章节的全部文本。索引中的页码即 PDF 页码，提取时 `起始页-1` 对应 PyMuPDF 的 0-based 索引。

**注意**：PPT 类 PDF（如 `12肝脏疾病.pdf`）文字较少，以提取图片为主；课本 PDF 的文字是可选中文本，适合直接提取。

### 内容重心

- **临床表现**、**病因与危险因素**：最详细，分系统展开
- **发病机制/病理生理**：适中，关键机制用箭头图或简表
- **诊断与辅助检查**：诊断标准、关键检查，鉴别诊断列表
- **治疗**：只写核心治疗原则和标准方案，不展开用药细节
- **预后/预防**：一两句话

### 四个核心学科

| 学科 | 课本目录 | 模板 | MOC | 大纲 |
|------|----------|------|-----|------|
| 内科学 | `02 Courses/内科学/` | `03 Templates/内科.md` | `00 MOC/内科学.md` | `内科学教学大纲.md` |
| 外科学 | `02 Courses/外科学/` | `03 Templates/外科.md` | `00 MOC/外科学.md` | `外科学教学大纲.md` |
| 妇产科学 | `02 Courses/妇产科学/` | `03 Templates/妇产科.md` | — | `妇产科学教学大纲.md` |
| 儿科学 | `02 Courses/儿科学/` | `03 Templates/儿科.md` | — | `儿科学教学大纲.md` |

## 笔记格式规范

- **Frontmatter**：必须包含 `tags`（学科标签）、`en`（英文标题）、`reviewed: false`（默认未审核）
- **标题**：不使用数字序号（如"一、"），直接用疾病/主题名
- **不保留**授课教师信息、课程来源和时间信息行
- **图片格式**：仅 `.webp`，命名规则 `学科-疾病-内容描述.webp`
- **Wikilink**：用于主题间的交叉引用跳转
- **表格**：大量使用表格呈现分类和对比信息
- **语言**：中文笔记，英文术语通过 frontmatter 的 `en:` 字段标记
- **笔记写入** `01 Notes/`，MOC 写入 `00 MOC/`，不得写入 `02 Courses/`

## 配图工具链

从 PDF/PPT 提取插图：
```bash
python -c "<PyMuPDF 提取页面为 PNG>" && magick <temp.png> <目标.webp>
```
- 图片输出到 `04 Attachments/`
- 使用 PyMuPDF (`fitz`) 以 2x 分辨率渲染
- 使用 ImageMagick (`magick`) 将 PNG 转换为 WebP
- 嵌入语法：`![[04 Attachments/文件名.webp]]`

## Obsidian 配置要点

- 默认笔记位置：`01 Notes/`（`.obsidian/app.json` → `newFileFolderPath`）
- 附件位置：`04 Attachments/`（`attachmentFolderPath`）
- 始终更新链接：`alwaysUpdateLinks: true`
- 严格换行：`strictLineBreaks: true`
- 显示行号：`showLineNumber: true`
- 实时预览：关闭（`livePreview: false`）

## Claudian 集成

此 vault 通过 Claudian (Obsidian 的 Claude 插件) 与 Claude Code 集成，使用 DeepSeek API 作为后端（模型：`deepseek-v4-flash`），权限模式为 yolo。配置位于 `.claudian/claudian-settings.json`。
